POST /reservations accepts
```
room_id
```

POST /rooms/{id} returns 200 OK
```
reservation_id
student_name
passport_number

room_type
reservation_duration
people_allowed_number

price
currency

room

banks
    bank_name
	account_name
	account_num
	iban
	swift
	currency

reciepts
    reciept_url
	upload_reciept_date (ordered by recent date)

is_confirmed
confirmation_deadline

is_rejected

last_update
follow_up_message
```

POST /rooms/{id} returns 400 Bad request
```
quota is full, try another room
```

POST /rooms/{id} returns 401 Unauthorized
```
please login before making a reservation
```


GET /user/reservation/{id} returns
```
student_name
passport_number

room_type
reservation_duration
people_allowed_number

price
currency

room

banks
    bank_name
	account_name
	account_num
	iban
	swift
	currency

reciepts
    reciept_url
	upload_reciept_date (ordered by recent date)

is_confirmed
confirmation_deadline

is_rejected

last_update
follow_up_message
```

POST /user/reservation/{id} accepts
```
reciepts
```