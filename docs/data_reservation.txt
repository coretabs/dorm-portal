POST /reservations accepts
```
room_id
```

POST /reservations returns 200 OK
```
reservation_id
room_id

student_name

room_type
reservation_duration
people_allowed_number

price
currency

banks
    bank_name
	account_name
	account_num
	iban
	swift
	currency

reciepts
    reciept_url
	upload_reciept_date (ordered by recent date)

status (pending=1, rejected=2, confirmed=3, waiting_manager_action=4 (student uploaded reciept), manager_updated=5, expired=6)

confirmation_deadline

last_update
follow_up_message
```

POST /reservations returns 400 Bad request
```
quota is full, try another room
```

POST /reservations returns 401 Unauthorized
```
please login before making a reservation
```


GET /reservations/{id} returns 200 OK
```
room_id
dorm_contact_name
dorm_contact_email
dorm_contact_number
dorm_contact_fax

student_name

room_type
reservation_duration
people_allowed_number

price
currency

banks
    bank_name
	account_name
	account_num
	iban
	swift
	currency

reciepts
    reciept_url
	upload_reciept_date (ordered by recent date)

status (pending=1, rejected=2, confirmed=3, waiting_manager_action=4 (student uploaded reciept), manager_updated=5, expired=6)

confirmation_deadline_date

last_update_date
follow_up_message
```

POST /reservations/{id}/reciept accepts
```
uploaded_reciept
```

POST /reservations/{id}/reciept returns 201 created
```
```

POST /reservations/{id}/reciept returns 400 Bad request
```
You can only add a photo for a reservation you own
```

POST /reservations/{id}/reciept returns 403 forbidden
```
You cant add a photo to this reservation
```